package schema

var (

	// Response schema
	Response = `{
		"active": "failed",
		"message": "Params are not valid",
		"payload": {
			"errors": [
				"(root): Must validate one and only one schema (oneOf)"
			]
		}
	}`
)
